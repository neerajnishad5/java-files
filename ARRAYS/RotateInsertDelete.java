public class RotateInsertDelete {
    public static void main(String[] args) {
        // int a[] = { 5, 9, 6, 10, 12, 7, 3, 5, 4, 2 };

        // left rotate array

        // for (int x : a) {
        // System.out.print(x + " ");
        // }
        // int temp = a[0];
        // System.out.println();
        // for (int i = 1; i < a.length; i++) {
        // a[i - 1] = a[i];
        // }

        // a[a.length - 1] = temp;

        // for (int x : a) {
        // System.out.print(x + " ");
        // }

        // System.out.println();

        // // right rotate array
        // int[] b = { 5, 9, 6, 10, 12, 7, 3, 5, 4, 2 };
        // for (int x : b) {
        // System.out.print(x + " ");
        // }

        // int temp1 = b[b.length - 1];

        // for (int i = b.length; i > 0; i--) {
        // b[i - 1] = b[i - 2];
        // }

        // b[0] = temp1;
        // System.out.println();
        // for (int x : b) {
        // System.out.print(x + " ");
        // }

        // INSERTING an ELEMENT at given INDEX

        // int A[] = new int[10];
        // A[0] = 5;
        // A[1] = 9;
        // A[2] = 6;
        // A[3] = 10;
        // A[4] = 12;
        // A[5] = 7;

        // int x = 15, n = 6, index = 2;

        // for (int i = n; i > index; i--)
        // A[i] = A[i - 1];

        // A[index] = x;

        // for (int y : A)
        // System.out.print(y + " ");

        // DELETE AN ELEMENT FROM ARRAY

    //     int c[] = { 5, 9, 6, 10, 12, 7, 3, 5, 4, 2 };
    //     int n = c.length;
    //     for (int x : c)
    //         System.out.print(x + " ");
    //     int index = 1;
    //     System.out.println();
    //     for (int i = 1; i < n-1; i++)
    //         c[i] = c[i + 1];


    //    for(int i =0;i<n-1;i++)
    //         System.out.print(c[i]+ " ");

    }

}
